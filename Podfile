# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CombineSampler' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'OpenCombine'
  pod 'OpenCombineDispatch'
  pod 'OpenCombineFoundation' # , :git => 'https://github.com/broadwaylamb/OpenCombine.git', :branch => 'master'

  # Pods for CombineSampler

  target 'CombineSamplerTests' do
    inherit! :search_paths
    pod 'Quick'
    pod 'Nimble'
  end

  target 'CombineSamplerUITests' do
    # Pods for testing
  end

end
